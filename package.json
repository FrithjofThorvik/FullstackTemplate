{
	"name": "root",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"web": "cd frontend && npm i && npm run build && npm start",
		"server": "cd backend && npm i && npm start",
		"deploy": "cp -a frontend/build/. public/",
		"buildncopy": "cd frontend && npm i && npm run build && (cp -r build ../backend/)",
		"heroku-postbuild": "npm run buildncopy"
	}
}
